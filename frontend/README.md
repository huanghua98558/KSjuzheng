# 快手短剧影视精灵后台前端源码

这是新建的 Vue 3 + Vite + Element Plus 源码工程，用来替代直接修改线上打包 JS 的方式。

## 命令

```bash
npm install
npm run dev
npm run build
```

开发环境默认把 `/api` 代理到 `https://mcn.kuaimax.cn`。

## 第一阶段范围

- 登录页
- 后台主布局
- 当前后台菜单和路由
- 通用数据表页面
- 系统设置页
- 默认权限配置中文显示

后续逐页把当前静态包功能还原为真正 `.vue` 源码。
